# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=currency_exchange_test

# External APIs (use mock URLs for testing)
COUNTRIES_API_URL=https://restcountries.com/v2/all
EXCHANGE_API_URL=https://open.er-api.com/v6/latest/USD

# Image Configuration
IMAGE_PATH=./cache/test
IMAGE_NAME=summary_test.png